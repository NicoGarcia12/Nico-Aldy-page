export interface LoginFormData {
  firstKissDate: string;
  anniversaryDate: string;
  firstSeries: string;
  ratedBookWord: string;
  sharedWord: string;
}

export interface SessionData {
  name: string;
}
